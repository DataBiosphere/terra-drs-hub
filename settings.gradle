pluginManagement {
	repositories {
		resolutionStrategy {
			eachPlugin {
				if (requested.id.id == 'bio.terra.test-runner-plugin') {
					useModule("bio.terra:terra-test-runner:${requested.version}")
				}
			}
		}
		mavenCentral()
		maven {
			url = 'https://us-central1-maven.pkg.dev/dsp-artifact-registry/libs-snapshot/'
		}
		gradlePluginPortal()
	}
}

rootProject.name = 'drshub'
include 'service', 'integration', 'client-resttemplate'
